services:
  client:
    build:
      context: ./client
      args:
        VITE_API_URL: ${VITE_API_URL:-https://bfflix.onrender.com}
    container_name: bfflix-client
    ports:
      - "4173:80"
    restart: unless-stopped
